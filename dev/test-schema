#!/usr/bin/env bash

set -eu -o pipefail

here=$(dirname -- "$0")

export DATABASE_URL=$(pg_tmp)
schema="$here"/../schema
psql -X "$DATABASE_URL" \
	-f "$schema"/extensions.sql \
	-f "$schema"/schema.sql
